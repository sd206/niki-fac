# Shared local development defaults. Copy to .env where a service reads it.
# Each app/service also has its own .env.example with scoped variables.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/niki
REDIS_URL=redis://localhost:6379
